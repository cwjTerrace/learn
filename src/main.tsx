import React from "react";
import ReactDOM from "react-dom";
import Layout from "./layout";

ReactDOM.render(
  <React.StrictMode>
    <Layout>1231231</Layout>
  </React.StrictMode>,
  document.getElementById("root")
);
